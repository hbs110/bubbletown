From 2e4462237d31f0f0027652f7d96e191ade29ceb7 Mon Sep 17 00:00:00 2001
From: Gu Lu <gemfeeling@hotmail.com>
Date: Mon, 19 Jan 2015 20:16:45 +0800
Subject: [PATCH] [coco-change] expose tileToNodeTransform() in
 CCFastTMXLayer.h

---
 cocos/2d/CCFastTMXLayer.h | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/cocos/2d/CCFastTMXLayer.h b/cocos/2d/CCFastTMXLayer.h
index d50a013..e8065de 100644
--- a/cocos/2d/CCFastTMXLayer.h
+++ b/cocos/2d/CCFastTMXLayer.h
@@ -177,6 +177,8 @@ public:
     virtual void draw(Renderer *renderer, const Mat4& transform, uint32_t flags) override;
     void removeChild(Node* child, bool cleanup = true) override;
 
+    Mat4 tileToNodeTransform();
+
 protected:
 
     bool initWithTilesetInfo(TMXTilesetInfo *tilesetInfo, TMXLayerInfo *layerInfo, TMXMapInfo *mapInfo);
@@ -186,7 +188,6 @@ protected:
     /* The layer recognizes some special properties, like cc_vertez */
     void parseInternalProperties();
     
-    Mat4 tileToNodeTransform();
     Rect tileBoundsForClipTransform(const Mat4 &tileToClip);
     
     int getVertexZForPos(const Vec2& pos);
-- 
1.8.3.msysgit.0

